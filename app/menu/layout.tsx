import type { Metadata } from "next";
export const metadata: Metadata = { title:"Brownie", description:"Explore the San Bakes Brownie preorder collection: signature brownies, boxes, Tins, Tubs, millet tea cakes and gifting formats." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
