import type { Metadata } from "next";
export const metadata: Metadata = { title:"Policies & Allergens", description:"Proposed San Bakes preorder, cancellation, delivery, pickup, quality, allergen and customer-information policies." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
