import type { Metadata } from "next";
export const metadata: Metadata = { title:"Preorder, Cancellation & Allergen Policies", description:"San Bakes preorder, cancellation, delivery, pickup, quality, allergen and customer-information policies.", alternates:{ canonical:"/policies" } };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
