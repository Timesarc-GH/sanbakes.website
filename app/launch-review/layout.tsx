import type { Metadata } from "next";
export const metadata: Metadata = { title:"Owner Launch Reference", description:"Private San Bakes approved V5 menu, quantities, prices and operating checks.", robots:{ index:false, follow:false } };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
