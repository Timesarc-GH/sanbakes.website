import type { Metadata } from "next";
export const metadata: Metadata = { title:"Frequently Asked Questions", description:"Answers about San Bakes ingredients, the current egg formulation, preorder timing, allergens, storage, Chennai delivery, pickup, changes and corporate orders." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
