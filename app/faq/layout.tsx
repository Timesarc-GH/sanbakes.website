import type { Metadata } from "next";
export const metadata: Metadata = { title:"Brownie Ordering FAQ — Chennai", description:"Answers about San Bakes Regular and Eggless formulations, ingredients, preorder timing, allergens, storage, Chennai delivery, pickup, changes and corporate orders.", alternates:{ canonical:"/faq" } };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
