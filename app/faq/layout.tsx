import type { Metadata } from "next";
export const metadata: Metadata = { title:"Frequently Asked Questions", description:"Answers about San Bakes ingredients, Egg and Eggless recipes, preorder timing, allergens, storage, delivery, pickup, changes and corporate orders." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
