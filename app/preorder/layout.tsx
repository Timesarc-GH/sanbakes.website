import type { Metadata } from "next";
export const metadata: Metadata = { title:"Preorder Enquiry", description:"Build a San Bakes preorder enquiry and send one structured request through WhatsApp for personal confirmation." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
