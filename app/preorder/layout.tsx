import type { Metadata } from "next";
export const metadata: Metadata = { title:"Cart & WhatsApp Checkout", description:"Review a San Bakes preorder cart, send the complete order through WhatsApp and generate an amount-linked UPI QR after confirmation." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
