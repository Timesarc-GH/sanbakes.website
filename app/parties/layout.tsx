import type { Metadata } from "next";

export const metadata: Metadata = { title:"Brownie Birthday & Party Orders in Chennai", description:"Preorder brownie birthday cakes, occasion cakes, individually packed brownies, Brownie Tins and Tubs for Chennai parties.", alternates:{ canonical:"/parties" } };

export default function PartiesLayout({ children }:{ children:React.ReactNode }) { return children; }
