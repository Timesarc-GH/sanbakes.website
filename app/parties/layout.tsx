import type { Metadata } from "next";

export const metadata: Metadata = { title:"Birthday & Party Orders", description:"Preorder brownie birthday cakes, occasion cakes, individually packed brownies, Brownie Tins and Tubs for Chennai parties." };

export default function PartiesLayout({ children }:{ children:React.ReactNode }) { return children; }
