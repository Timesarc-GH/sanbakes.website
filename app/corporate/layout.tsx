import type { Metadata } from "next";

export const metadata: Metadata = { title:"Corporate Orders", description:"Premium San Bakes brownie boxes, individually packed brownies, Tins, Tubs and bespoke corporate gifting for Chennai teams and clients." };

export default function CorporateLayout({ children }:{ children:React.ReactNode }) { return children; }
