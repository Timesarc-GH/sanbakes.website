import type { Metadata } from "next";
export const metadata: Metadata = { title:"Brownie Gift Boxes in Chennai", description:"Curated San Bakes brownie boxes, Tins and seasonal hampers for thoughtful personal gifting in Chennai.", alternates:{ canonical:"/gifting" } };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
