import type { Metadata } from "next";
export const metadata: Metadata = { title:"Our Standard", description:"How San Bakes approaches small-batch production, ingredient clarity, allergens, honest claims and preorder capacity." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
