import type { Metadata } from "next";
export const metadata: Metadata = { title:"Delivery & Pickup", description:"San Bakes delivery within Chennai, with distance-based charges beyond 20 km, plus appointment pickup guidance." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
