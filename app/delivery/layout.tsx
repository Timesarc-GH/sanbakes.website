import type { Metadata } from "next";
export const metadata: Metadata = { title:"Delivery & Pickup", description:"San Bakes delivery within a validated 20 km road radius from Keelkattalai, Chennai, plus appointment pickup guidance." };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
