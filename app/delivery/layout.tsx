import type { Metadata } from "next";
export const metadata: Metadata = { title:"Brownie Delivery & Pickup in Chennai", description:"San Bakes delivery within Chennai, with distance-based charges beyond 20 km, plus appointment pickup guidance.", alternates:{ canonical:"/delivery" } };
export default function Layout({ children }:{ children:React.ReactNode }) { return children; }
